# 电子教材维护指南

这个网站是纯静态页面。解压后直接打开 `index.html` 可以预览；上传到 GitHub Pages 时，无需构建、安装依赖或运行服务器。

## 要改什么，找哪个文件

| 修改内容 | 文件 |
| --- | --- |
| 某一课的练习表达、单词、听力题、选项、正确答案、听力原文 | `data/lessons/lesson-01.js` ～ `lesson-05.js` |
| 第一课之前的 HSK 介绍和使用方法 | `index.html` 中的 `<template id="intro-template">` |
| 页面互动：显示开关、随机出题、播放与暂停、答题和重做 | `app.js` |
| 颜色、字号、卡片和手机排版 | `style.css`（按功能有分区注释） |
| 录音 | `assets/audio/dg2/` |
| 网页骨架、脚本加载顺序 | `index.html` |

每课只有**一个**内容文件。正确答案就在对应题目的 `correct` 字段中，不再另设答案表，也没有加载时覆盖题目的修订文件。例如要改第四课第 37 题，请搜索 `data/lessons/lesson-04.js` 里的 `"number": 37`。

## 修改题目

题目字段示例（仅展示格式）：

```json
{
  "number": 37,
  "type": "choice",
  "choices": {
    "A": "长不大",
    "B": "长不了大个",
    "C": "会变胖",
    "D": "会长得更高"
  },
  "correct": ["B"],
  "script": "这里是听力原文。"
}
```

- `number` 是题号；不要改变 1～45 的顺序。
- `type` 为 `choice`（四选一）或 `trueFalse`（正误判断）。正误题的答案写成 `"correct": ["true"]` 或 `["false"]`。
- `correct` 可以包含多个合理答案，例如 `["A", "C"]`。
- `script` 是学生答题后看到的听力原文。修改选项时，请同时核对答案与原文。
- 可选的 `statement` 是正误题显示的句子；四选一题通常没有这一字段。

## 修改单词和录音

单词字段：`zh` 为中文，`py` 为拼音，`ja` 为日语，`audioTrack` 为该课单词录音编号。例如 `"audioTrack": "9"` 会播放 `assets/audio/dg2/1k/t/dg2-1k-t-9.mp3`（第一课）。删除一个单词时，不要给后续词语重新编号，保持它们与原录音一一对应。

练习表达按列表顺序对应 `p` 目录中的录音：第一课第 2 条对应 `assets/audio/dg2/1k/p/dg2-1k-p-2.mp3`。听力题录音在 `l` 目录；第 36、37 题共用文件名末尾为 `36.37.mp3` 的录音。替换录音时保持原文件名即可。

当前教材共有 5 课、37 条练习表达、182 个单词、225 道听力题。第一课单词表不含「害羞」。

## 修改后检查

1. 保存文件为 UTF-8 编码。单词、题目等数据文件要保留外层的 `window.TEXTBOOK_DATA...` 代码和引号、逗号。
2. 如电脑装有 Python 3.10 或更新版本，在解压后的教材目录运行 `python tools/check_content.py`（Windows 也可试 `py tools/check_content.py`）。它会检查题号、答案和录音文件是否齐全。
3. 打开 `index.html`，实际点开修改过的课和题目，播放录音并试答；在手机或窄浏览器窗口也看一下排版。
4. 把更新后的**文件和文件夹**上传到 GitHub 仓库根目录。不要只上传 ZIP。GitHub Pages 会继续使用原来的发布设置。

`tools/check_content.py` 是可选的离线检查工具，不参与网页运行。原始 PDF 在 `assets/動画2テキスト.pdf`，网页显示的数据在每课文件中，修改网页题目不会修改 PDF。
